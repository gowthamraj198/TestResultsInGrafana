package pages

import java.util.concurrent.TimeUnit

import org.openqa.selenium.support.{FindBy, PageFactory}
import org.openqa.selenium.{WebDriver, WebElement}

class BasePage(driver: WebDriver) {

  def waitForElement(): Unit = {

  }
}
